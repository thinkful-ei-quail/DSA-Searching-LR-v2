class BST {
  constructor(key) {
    this.key = key;
    this.left = null;
    this.right = null;
  }
}

function postToBst(arr) {
  const root = arr[arr.length - 1];
}

console.log(postToBst([8, 12, 10, 16, 25, 20, 15]));
